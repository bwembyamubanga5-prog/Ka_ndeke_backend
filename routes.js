// routes.js — compatibility shim that delegates to routes-index.js
module.exports = require('./routes-index');

// gameEngine.js — compatibility shim that delegates to gameEngine.js
module.exports = require('./gameEngine');
